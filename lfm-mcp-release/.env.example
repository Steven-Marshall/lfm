# LFM MCP Server - Environment Configuration Template
# Copy this file to .env and fill in your values

# ============================================
# AUTHENTICATION (Choose ONE method)
# ============================================

# Option 1: OAuth 2.0 with GitHub (RECOMMENDED)
# Create GitHub OAuth App: https://github.com/settings/developers
# - Application name: LFM MCP Server
# - Homepage URL: Your server base URL
# - Authorization callback URL: {BASE_URL}/oauth/callback
GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here
BASE_URL=https://your-server.example.com

# Optional: Restrict access to specific GitHub usernames (comma-separated)
# ALLOWED_GITHUB_USERS=username1,username2

# Option 2: Bearer Token (LEGACY - fallback if OAuth not configured)
# Generate a secure random token: openssl rand -base64 32
# LFM_AUTH_TOKEN=your_secure_random_token_here

# Option 3: No Authentication (NOT RECOMMENDED for production)
# Simply leave all auth variables commented out

# ============================================
# BUILD CONFIGURATION
# ============================================

# Target architecture for .NET binary
# Options: linux-x64, linux-arm64
# Default: linux-x64 (for local Windows/Intel testing)
# For Spark/ARM64: Set to linux-arm64
TARGET_ARCH=linux-x64

# ============================================
# RUNTIME CONFIGURATION (Optional)
# ============================================

# HTTP Server Port (default: 8002)
# HTTP_PORT=8002

# CORS allowed origins (comma-separated, default: *)
# ALLOWED_ORIGINS=*

# Session timeout in minutes (default: 480 = 8 hours)
# SESSION_TIMEOUT_MINUTES=480

# ============================================
# CONFIGURATION MANAGEMENT
# ============================================
# All other settings (Last.fm, Spotify, Sonos, cache, etc.)
# are stored in config.json which is mounted directly into the container.
#
# To manage config.json:
#   1. Use the lfm CLI on host: lfm config set-api-key <key>
#   2. Export to Docker: lfm config export --to-docker
#
# The config.json file is mounted read-only at:
#   ./config.json:/app/config/config.json:ro
